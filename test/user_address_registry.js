

contract('UserAddressRegistry', function(accounts) {
  it("should assert true", function(done) {
    var user_address_registry = UserAddressRegistry.deployed();
    assert.isTrue(true);
    done();
  });
});
